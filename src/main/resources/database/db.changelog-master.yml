databaseChangeLog:
  # Далее указываются "include" свойства со путями к файлом, содержащими changesets
  # В рамках разработки небольшого приложения можно оставить один файл,
  # продолжая и далее наполнять его
  - include:
      file: database/sql/init.sql
